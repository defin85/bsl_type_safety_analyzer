Процедура ТестоваяПроцедура19()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 19
    Результат = Запрос.Выполнить();
КонецПроцедуры
