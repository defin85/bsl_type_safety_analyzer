Процедура ТестоваяПроцедура12()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 12
    Результат = Запрос.Выполнить();
КонецПроцедуры
