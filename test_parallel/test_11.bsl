Процедура ТестоваяПроцедура11()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 11
    Результат = Запрос.Выполнить();
КонецПроцедуры
