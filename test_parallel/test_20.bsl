Процедура ТестоваяПроцедура20()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 20
    Результат = Запрос.Выполнить();
КонецПроцедуры
