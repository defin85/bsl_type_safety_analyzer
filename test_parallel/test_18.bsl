Процедура ТестоваяПроцедура18()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 18
    Результат = Запрос.Выполнить();
КонецПроцедуры
