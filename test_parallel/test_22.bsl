Процедура ТестоваяПроцедура22()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 22
    Результат = Запрос.Выполнить();
КонецПроцедуры
