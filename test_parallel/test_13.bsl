Процедура ТестоваяПроцедура13()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 13
    Результат = Запрос.Выполнить();
КонецПроцедуры
