Процедура ТестоваяПроцедура10()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 10
    Результат = Запрос.Выполнить();
КонецПроцедуры
