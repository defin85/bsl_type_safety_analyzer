Процедура ТестоваяПроцедура17()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 17
    Результат = Запрос.Выполнить();
КонецПроцедуры
