Процедура ТестоваяПроцедура16()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 16
    Результат = Запрос.Выполнить();
КонецПроцедуры
