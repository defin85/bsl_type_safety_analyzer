Процедура ТестоваяПроцедура24()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 24
    Результат = Запрос.Выполнить();
КонецПроцедуры
