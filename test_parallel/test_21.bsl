Процедура ТестоваяПроцедура21()
    Перем Таблица, Запрос;
    
    Таблица = Новый ТаблицаЗначений();
    Запрос = Новый Запрос();
    
    // Тестовый код 21
    Результат = Запрос.Выполнить();
КонецПроцедуры
