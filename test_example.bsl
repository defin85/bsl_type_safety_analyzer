// Тестовый BSL файл для проверки анализатора
Процедура ТесtoваяПроцедура() Экспорт
    Перем Переменная1;
    Перем Переменная2;
    
    Переменная1 = "Тестовое значение";
    // Переменная2 не используется - должно быть предупреждение
    
    Сообщить(Переменная1);
КонецПроцедуры

Функция ПолучитьТестовоеЗначение()
    Перем Результат;
    Результат = 42;
    Возврат Результат;
КонецФункции