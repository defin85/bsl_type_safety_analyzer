// Тестовый модуль BSL для проверки анализатора
// Test BSL module for analyzer verification

#Область ПубличныйИнтерфейс

// Простая процедура с параметрами
Процедура ВывестиСообщение(Знач ТекстСообщения)
    Сообщить(ТекстСообщения);
КонецПроцедуры

// Функция с возвращаемым значением
Функция ПолучитьТекущуюДату()
    Возврат ТекущаяДата();
КонецФункции

// Функция с логикой и циклами
Функция НайтиЭлементВМассиве(Массив, ИскомыйЭлемент)
    Для Каждого Элемент Из Массив Цикл
        Если Элемент = ИскомыйЭлемент Тогда
            Возврат Истина;
        КонецЕсли;
    КонецЦикла;
    
    Возврат Ложь;
КонецФункции

// Процедура с обработкой ошибок
Процедура ВыполнитьОперацию()
    Попытка
        // Какая-то операция
        Результат = ПолучитьТекущуюДату();
        ВывестиСообщение("Операция выполнена успешно: " + Результат);
    Исключение
        ВывестиСообщение("Ошибка при выполнении операции: " + ОписаниеОшибки());
    КонецПопытки;
КонецПроцедуры

#КонецОбласти

#Область СлужебныйИнтерфейс

// Приватная функция
Функция ВнутренняяФункция(Параметр1, Параметр2)
    Если НЕ ЗначениеЗаполнено(Параметр1) Тогда
        Возврат Неопределено;
    КонецЕсли;
    
    Возврат Параметр1 + Параметр2;
КонецФункции

#КонецОбласти
